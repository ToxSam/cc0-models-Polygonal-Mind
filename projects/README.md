# GLB Output

Converted GLB/glTF files from the Polygonal Mind asset packs. Each pack has its own subfolder.

When a pack is done, move its **source** folder from `../PolygonalMind_OpenSource/` to `../completed-sources/` and update `../projects.md`.
